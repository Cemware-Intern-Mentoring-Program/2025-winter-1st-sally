# 1-2. 데이터베이스와 JPA

# 이번 주제

* 데이터베이스는 무엇이고 어떤 종류가 있나요?  또, 관계형 데이터베이스란 무엇인가요?
* ORM이란 무엇일까요? 스프링에서 ORM을 어떻게 이용할 수 있을까요?
* Entity의 생명주기는 어떻게 되고, 관련 어노테이션은 무엇이 있나요?
* 연관관계 매핑은 무엇이고 어떻게 이용할 수 있을까요?


# 발표 내용

## 데이터베이스는 무엇이고 어떤 종류가 있나요? 또, 관계형 데이터베이스란 무엇인가요?

**👽 데이터베이스의 필요성**
- 많은 데이터를 안전하게 저장, 조회, 수정, 삭제하기 위해
- 동시성 제어, 트랜잭션 보장, 영속 저장, 대규모 데이터 처리를 안정적으로 수행

> ⭐ 정리: 데이터베이스란, 많은 데이터를 안전하고, 빠르고 일관성있게 저장, 조회, 수정, 삭제할 수 있게 만든 전문 시스템

**👽 데이터베이스의 종류**
1. 관계형 데이터베이스(RDBMS)
2. NoSQL 데이터 베이스

**👽 관계형 데이터베이스(RDBMS)**
- 예: MySQL, PostgreSQL, Oracle, SQL Server
- 테이블 구조: 행과 열(row and column)
- 스키마 기반: 각 테이블의 구조(컬럼 이름, 타입 등)가 미리 정의
- SQL 이라는 표준 언어로 조회/저장/수정/삭제
- 트랜잭션, 무결성, 참조 관계 지원이 매우 강력

**👽 NoSQL 데이터 베이스**
- 스키마가 유연하거나, 대규모 분산 시스템을 위해 등장한 DB
- 유연한 구조
- 비정형 데이터에 강함
- 문서 (Document) DB
   - 예: MongoDB
   - JSON과 비슷한 문서 형태로 저장
   - 컬렉션 안에 문서들이 들어있는 구조
- 키-값(Key-Value) DB
   - 예: Redis
   - Key와 Value의 단순 매핑
   - 캐시, 세션과 같은 간단하고 빠른 저장용
- Wide-Column/Column Family DB
   - 예: Cassandra, HBase
   - 컬럼 단위 저장에 최적화
   - 아주 많은 데이터 처리에 강함
- 그래프 DB
   - 예: Neo4j
   - 노드와 간선으로 관계 표현

**👽 관계형 데이터 베이스 ?**
- 데이터를 테이블 단위로 관리
   - 엑셀 표와 비슷...
- 테이블을 PK-FK 관계로 연결
   - PK(Primary Key): 각 행을 유일하게 구분하는 값
   - FK(Foreign Key): 다른 테이블의 PK를 참조하는 값
- 테이블 간의 연결 = 관계(Relation) ⇒ **관계형 데이터베이스**

**👽 관계형 데이터베이스 특징**
- 정합성(ACID)을 강하게 보장
- SQL 기반으로 명확한 구조 유지

**👽 ERD(Entity Relationship Diagram) ?**
- 여러 테이블이 서로 관계를 맺어 복잡한 구조를 가지면, 관리 힘듬 → ERD 등장
- 데이터베이스 구조를 시각적으로 표현하는 다이어그램
- Entity와 그 사이의 관계(Relationship)을 그림으로 표현한 데이터베이스 설계도
- DB 설계 도면
- 개발자들이 같은 구조를 공유하기 좋음
- 실제 테이블 생성, Entity 클래스 설계, 연관관계 매필 설계 가능

**👽 ERD(Entity Relationship Diagram)의 구성요소**
1. Entity(엔티티)
2. Attribute(속성)
3. Relationship(1:1, 1:N, N:M등 관계)
4. Cardinality(키디널리티, 관계 수)

**👽 Entity(엔티티)**
- 실제 DB의 테이블
- ERD에서 네모박스로 표현
- 예: Member, Order, Product 등

**👽 Attribute(속성)**
- Entity가 가진 Column들
- ERD에서 타원으로 표현하여 연결
- 예: Member의 id, name, email 등
- 특수 속성
- PK(Primary Key, 기본키): Entity의 대표 키
- FK(Foreign Key, 외래키): 다른 Entity 참조

**👽 Relationship(관계)**
- 엔티티끼리 어떻게 연결되어 있는지 표시
- ERD에서 선으로 엔티티를 연결하고, 중간에 1, N 등 관계 수(카디널리티)를 나타냄
- 예: Member 1 --- N Order , 한 명의 회원은 여러 개의 주문을 할 수 있다.

**👽 Cardinality(카디널리티, 관계 수)**
- 1 : 1
- 1 : N
- N : M


## ORM은 무엇일까요? 스프링에서 ORM을 어떻게 이용할 수 있을까요?

**👽 ORM의 필요성: 자바와 DB의 모델 불일치(객체-관계 불일치)**
-

**👽 ORM ?**



1. ORM은 무엇이고 왜 필요할까요?
2. 스프링에서 ORM을 어떻게 활용하나요?

   
   1. Spring Data JPA란 무엇일까요?
   2. Repository Layer란 무엇일까요?
   3. JpaRepository Interface에는 어떤 기능들이 포함되어 있나요?

## Entity는 무엇이고 생명주기는 어떻게 되나요? 관련 어노테이션은 무엇이 있을까요?


1. Entity는 무엇인가요?
2. 영속성은 무엇일까요?

   
   1. Entity의 생명주기는 어떻게 되나요?
   2. Entity에 관련된 어노테이션은 무엇이 있나요?

## 연관관계 매핑은 무엇이고 어떻게 이용할 수 있을까요?


1. 연관관계 매핑은 왜 이용할까요?
2. 연관관계 매핑에는 무엇이 있을까요?
3. 영속성 전이는 무엇이고 어떻게 활용할 수 있을까요?

# 이번 과제


> ERD를 작성하며 데이터베이스를 설계해보고 Entity 객체와 Repository Layer를 구성해보는 시간을 가져요

# 같이 고민해봐요
> Entity Class를 깔끔하게 구현해봅시다. 다음과 같은 이유를 생각해보며 클래스를 구현해봐요 1. Setter는 왜 지양해야 할까요? 2. 기본 생성자의 접근 지정자를 제한하는게 좋습니다. 이유가 뭘까요? 또, 어떻게 접근 지정자를 제한할 수 있을까요? 3. 생성자 패턴에는 다양한 방법이 있습니다. 생성자, 팩토리 메서드, 빌더 패턴이 있는데 이 패턴들은 무엇이고 각자 어떤 장단점이 있을까요?
